"""Canonical channel registry domain — one channel_id contract across PreM3."""

from __future__ import annotations

# Implementations land in M5-01 (models, registry loader, validation, compiler).

__all__: list[str] = []
